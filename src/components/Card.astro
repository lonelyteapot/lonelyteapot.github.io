<div class="card">
    <slot />
</div>

<style>
    .card {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        padding: 1rem;
        border-radius: 8px;
        background-color: var(--color-canvas);
        box-shadow: var(--shadow-md);
    }

    .card :global(h3, h4, h5, h6) {
        margin: 0;
    }

    .card :global(ul) {
        margin: 0;
        padding: 0;
    }
</style>
